# Framework web
Flask==3.1.2
itsdangerous>=2.2.0   # Compatible con Flask 3.1.2
Jinja2>=3.1.2,<4.0.0
Werkzeug>=2.3.0,<3.0.0
click>=8.1.3,<9.0.0
blinker>=1.9.0

# Manejo de variables de entorno
python-dotenv==1.0.0

# Requests para llamadas HTTP
requests==2.32.0

# Stripe
stripe==12.5.1

# Gemini (solo si usas el paquete oficial de Google Gemini AI)
# Como no existe gemini==0.1.0, se usa la versión más reciente disponible compatible
gemini>=0.30.2,<0.31.0

# CORS si planeas recibir llamadas desde frontend
flask-cors==4.0.0

# Tipos y desarrollo
typing-extensions==4.7.1

# Seguridad / autenticación
bcrypt==4.1.0
