# Framework web
Flask==3.1.2

# Variables de entorno
python-dotenv==1.0.0

# Peticiones HTTP
requests==2.32.0

# Stripe para pagos
stripe==12.5.1

# API de Gemini (IA)
gemini==0.30.2

# Librerías útiles adicionales
gunicorn==21.2.0      # Servidor WSGI para despliegue en producción
Werkzeug==2.3.8       # Dependencia de Flask
Jinja2==3.1.2         # Motor de templates de Flask
click==8.2.0          # Utilidades de línea de comandos
itsdangerous==2.1.2   # Seguridad en Flask
markupsafe==2.1.3     # Para plantillas seguras
